---
interface Props {
  url: string;
}
const { url } = Astro.props;
---

<section class="w-full pb-16 flex justify-center items-center z-20 relative">
  <div class="w-full">
    <img loading="lazy" class="" src={url} alt="" />
  </div>
</section>
